import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anwarnnnot.mydashboard',
  appName: 'my-dashboard',
  webDir: 'dist'
};

export default config;
